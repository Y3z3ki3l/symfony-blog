<div class="col-lg-6">
    <ul class="list-unstyled">
    	<li><a href="{{path('main_default_index')}}">Todos</a></li>
		{% for category in categories %}
		    <li><a href="{{path('main_default_index_1', {'categoryCode': category.code})}}">{{category.name}}</a></li>
		{% endfor %}
    </ul>
</div>